# Netflix-clone

This is a Netflix clone application built with React and Vite.

## Features
- Dynamic movie listings
- Hero section with featured content
- Responsive design
- Video player modal

## Getting Started
To run the application locally:
1. Clone the repository
2. Run `npm install`
3. Run `npm run dev`
